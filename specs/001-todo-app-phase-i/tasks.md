---description: "A checklist for Claude Code to execute step-by-step."---# Implementation Tasks- [ ] Task 1: Environment Setup  - Initialize UV project (`uv init`).  - Add dependencies (`uv add rich`).  - Create directory structure (`src/`).- [ ] Task 2: Define Models  - Create `src/models.py`.  - Implement `Task` dataclass with type hints and UUID generation.- [ ] Task 3: Implement Service Logic  - Create `src/service.py`.  - Implement `TodoService` class.  - Implement Add, View, Update, Delete, and Complete logic.  - Ensure operations manipulate the in-memory list correctly.- [ ] Task 4: Implement UI Helpers  - Create `src/ui.py`.  - Create a function to display the Task List using `rich.console.Console` and `rich.table.Table`.  - Create input prompts for capturing task details.- [ ] Task 5: Main Application Loop  - Create `src/main.py`.  - Wire together the UI and Service.  - Implement the main menu logic (While loop).  - Handle "Exit" condition.- [ ] Task 6: Verification  - Run the app via `uv run src/main.py`.  - Verify all 5 basic features work as expected.